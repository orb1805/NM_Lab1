<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="e.roman.nm_lab1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NM_Lab1">
        <activity android:name=".QRSolveActivity" />
        <activity android:name=".JacobiSolveActivity" />
        <activity android:name=".SeidelSolveActivity" />
        <activity android:name=".FixedPointIterationActivity" />
        <activity android:name=".TrigiagonalSolveActivity" />
        <activity android:name=".LuSolveActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>